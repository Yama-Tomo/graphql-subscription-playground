require:
  - ts-node/register
schema:
  - './src/schema/index.ts':
      noPluck: true
generates:
  ./src/resolvers/generated.ts:
    config:
      contextType: ../context#Context
      scalars:
        Date: Date
        DateTime: Date
    plugins:
      - typescript
      - typescript-resolvers
