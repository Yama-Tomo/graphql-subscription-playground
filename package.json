{
  "name": "graphql-subscription-playground",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "pnpm --parallel dev --filter .",
    "lint": "pnpm --parallel lint --filter .",
    "format": "pnpm run format --filter ."
  },
  "author": "",
  "license": "MIT",
  "engines": {
    "node": "16",
    "pnpm": ">= 6.23.0"
  },
  "pnpm": {
    "overrides": {
      "graphql": "^16.0.1"
    }
  }
}
